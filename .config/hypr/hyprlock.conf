source = ~/.config/hypr/mocha.conf

general {
    grace = 3
    hide_cursor = true
    no_fade_in = false
    no_fade_out = false
    ignore_empty_input = true
    immediate_render = false
}

background {
    monitor =
    path = ~/Pictures/Wallpaper/Catppuccin-Mocha/tree.jpg
    blur_passes = 3
    blur_size = 3
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

# Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = $text
    font_size = 120
    font_family = JetBrains Mono Nerd Font
    position = 0, 300
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 5
    shadow_color = $crust
    shadow_boost = 1.2
}

# Date
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%A, %d %B %Y")"
    color = $subtext0
    font_size = 24
    font_family = JetBrains Mono Nerd Font
    position = 0, 180
    halign = center
    valign = center
}

# User
label {
    monitor =
    text = Hi, $USER
    color = $mauve
    font_size = 28
    font_family = JetBrains Mono Nerd Font
    position = 0, 80
    halign = center
    valign = center
}

# Input field
input-field {
    monitor =
    size = 300, 60
    outline_thickness = 2
    dots_size = 0.2
    dots_spacing = 0.35
    dots_center = true
    dots_rounding = -1
    outer_color = $mauve
    inner_color = $base
    font_color = $text
    fade_on_empty = false
    fade_timeout = 1000
    placeholder_text = <span foreground="#$textAlpha">Enter Password...</span>
    hide_input = false
    rounding = 8
    check_color = $yellow
    fail_color = $red
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_timeout = 2000
    fail_transitions = 300
    capslock_color = $peach
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false

    position = 0, -100
    halign = center
    valign = center
}

# Battery status (for laptop)
label {
    monitor =
    text = cmd[update:5000] cat /sys/class/power_supply/BAT*/capacity 2>/dev/null | head -1
    color = $green
    font_size = 16
    font_family = JetBrains Mono Nerd Font
    position = 30, 30
    halign = left
    valign = bottom
}

# Keyboard layout indicator
label {
    monitor =
    text = cmd[update:1000] hyprctl devices | grep -A 2 "Keyboard at" | grep "active keymap" | head -1 | awk '{print $NF}'
    color = $blue
    font_size = 16
    font_family = JetBrains Mono Nerd Font
    position = -30, 30
    halign = right
    valign = bottom
}
